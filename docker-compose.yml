services:
  dashboard:
    build: .
    image: mass-ai-dashboard:local
    env_file:
      - .env.example
    ports:
      - "8501:8501"
    volumes:
      - ./reports:/app/reports
      - ./shared/data:/app/shared/data:ro
    restart: unless-stopped
