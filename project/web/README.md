# Web App

This folder contains the current MASS-AI web runtime for `main`.

- `dashboard/app.py`
  Streamlit application entry point.
- `requirements.txt`
  Web dependency set for this branch.

Run directly with:

`streamlit run project/web/dashboard/app.py`
