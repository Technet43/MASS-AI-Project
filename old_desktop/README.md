# Old Desktop Version (Legacy - Transition Phase)

**Important 2026:** Core engine moved to `../shared/core/mass_ai_engine.py`.
This desktop app is legacy and is being migrated to use the shared engine exclusively.

- `mass_ai_desktop.py` → Tkinter analyst app (uses shared/core)
- `ui_kit.py` → Desktop-only UI components
- `requirements.txt` → DEPRECATED (use ../shared/requirements.txt)

This folder exists only during the ongoing de-duplication and architecture cleanup.
